# SQLite 数据库（默认本地文件）
DATABASE_URL="file:./dev.db"

# OpenAI 兼容 API（火山引擎 / OpenAI / 百炼等均可）
OPENAI_API_KEY=""
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_MODEL="gpt-4o-mini"
OPENAI_EMBEDDING_MODEL="text-embedding-3-small"

# 未配置 API Key 时自动进入演示模式（规则检索 + 模板回复）
